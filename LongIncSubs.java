package DSA;

public class LongIncSubs {
    private static int[] seq_ar;

    public LongIncSubs(int[] array) {
        longestSubSequence(array);
    }

    private static void longestSubSequence(int[] ar) {
        //        int max_len = 0;
        //        for (int i = 0; i < ar.length; i++) {
        //            max_len = Math.max(lss(ar, i), max_len);
        //        }
        //        seq_ar = new int[ar.length];
    }


}
